#include <stdio.h>
#include <string.h>
/**
 *main - entry function
 *Description : Function to print alphabets
 *Return : always 0 if successful
 */
int main(void)
{
char ch;
for(ch='a';ch<='z';ch++)
{
putchar(ch);
}
return (0);
}
