#include "main.h"
/**
 *mul - Function that returns the result of multiplication 
 *of a and b
 *@a: First parameter 
 *@b: Second parameter
 *Return: @a multiplied by @b
 */
int mul(int a, int b)
{
int result = a * b;
return (result);
}
