#include "main.h"
#include "string.h"
/**
 *rev_string - Function to print a s tring in reverse
 *@s: string to be passed.
 *
 *
 */
void rev_string(char *s)
{
int n = strlen(s);
while (n--)
{
_putchar(s[n]);
}
 _putchar(s[n]);
}
