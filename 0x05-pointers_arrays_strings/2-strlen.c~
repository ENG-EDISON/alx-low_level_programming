#include "main.h"
/**
 *_strlen - Function to find the length of a string
 *@s: is the string to be passed,or array of characters
 *
 */
int _strlen(char *s)
{
int n = 0;
while (s[n] != '\0')
{
n++;
}
return (n);
}
